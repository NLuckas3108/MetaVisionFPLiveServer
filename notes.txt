 docker run --gpus all \
  --network host \
  -v $(pwd):/workspace \
  -w /workspace \
  -it foundationpose:with_realsense \
  bash -c "export PATH=/opt/conda/envs/my/bin:$PATH && bash MetaVisionFPLiveServer/run.sh"
