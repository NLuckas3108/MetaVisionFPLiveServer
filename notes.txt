docker run --gpus all --network host \
  -v $(pwd):/workspace \
  -v /tmp/fp_shared:/data \
  -w /workspace \
  -it foundationpose:with_realsense \
  bash -c "export PATH=/opt/conda/envs/my/bin:$PATH && python MetaVisionFPLiveServer/mt_fp_live.py"
